# GPU Integration P0 Unlock Report

> **Status**: TEMPLATE - 待填充实测数据  
> **Purpose**: 标准化 GPU P0 验收输出  
> **Target**: 解锁进入 heterogeneous/ 阶段

---

## 1. 测试环境

| 项目 | 规格 | 备注 |
|:-----|:-----|:-----|
| **GPU 型号** | [待填: e.g., NVIDIA RTX 4090 x3] | 精确型号 |
| **驱动版本** | [待填: e.g., 535.104.05] | `nvidia-smi` 显示 |
| **CUDA 版本** | [待填: e.g., 12.2] | `nvcc --version` |
| **推理框架** | [待填: e.g., vLLM 0.4.0] | 精确版本 |
| **模型版本** | [待填: e.g., Nemotron-3-120B] | 权重版本/校验和 |
| **测试时间** | [待填: YYYY-MM-DD HH:MM] | 开始时间 |
| **测试主机** | [待填: hostname] | 机器标识 |

---

## 2. 负载配置

| 参数 | 值 | 说明 |
|:-----|:---|:-----|
| **Prompt 长度** | [待填: e.g., 2048 tokens] | 输入序列长度 |
| **Output 长度** | [待填: e.g., 512 tokens] | 输出序列长度 |
| **并发数** | [待填: e.g., 8] | 同时请求数 |
| **Batch Size** | [待填: e.g., dynamic/max 16] | GPU batch |
| **测试时长** | 3600 seconds (1 hour) | 强制 1 小时 |
| **预热时间** | 300 seconds (5 min) | 不计入统计 |

---

## 3. 核心指标 (必须全部满足)

### 3.1 GPU Utilization

| 统计量 | 阈值 | 实测值 | 状态 |
|:-------|:-----|:-------|:----:|
| **平均值** | > 60% | [待填] | ⏸️ |
| **P95 (最低容忍)** | > 50% | [待填] | ⏸️ |
| **最小值** | > 30% | [待填] | ⏸️ |
| **标准差** | < 20% | [待填] | ⏸️ |

**评判**: 不能只有瞬时峰值，必须 sustained load。

### 3.2 VRAM 使用

| 统计量 | 阈值 | 实测值 | 状态 |
|:-------|:-----|:-------|:----:|
| **峰值** | < 90% | [待填] | ⏸️ |
| **平均值** | < 80% | [待填] | ⏸️ |
| **释放后残留** | < 5% | [待填] | ⏸️ |

**评判**: 无 OOM，无显存泄漏。

### 3.3 吞吐性能

| 指标 | 阈值 | 实测值 | 状态 |
|:-----|:-----|:-------|:----:|
| **平均吞吐** | > 1000 tok/s | [待填] | ⏸️ |
| **P99 延迟** | < 5s | [待填] | ⏸️ |
| **吞吐塌陷次数** | = 0 | [待填] | ⏸️ |

**评判**: 无持续性性能塌陷。

### 3.4 稳定性

| 指标 | 阈值 | 实测值 | 状态 |
|:-----|:-----|:-------|:----:|
| **OOM 次数** | = 0 | [待填] | ⏸️ |
| **崩溃/重启次数** | = 0 | [待填] | ⏸️ |
| **人工干预次数** | = 0 | [待填] | ⏸️ |
| **错误率** | < 1% | [待填] | ⏸️ |

---

## 4. 原始数据日志

```bash
# 监控命令（测试期间运行）
nvidia-smi dmon -s pucm -d 1 -o DT > gpu_dmon.log &
# 测试完成后保存以下文件：
# - gpu_dmon.log
# - stress_test_output.json
# - error.log (如有)
```

**日志文件位置**: [待填: 路径]

---

## 5. 测试脚本

使用的测试脚本:
```bash
python3 gpu_stress_test.py \
  --model [MODEL_NAME] \
  --duration 3600 \
  --prompt-len [LEN] \
  --output-len [LEN] \
  --concurrency [N] \
  --target-util 0.60 \
  --max-vram 0.90 \
  --output gpu_integrity_report.json
```

---

## 6. 结论

| 检查项 | 要求 | 实测 | 通过 |
|:-------|:-----|:-----|:----:|
| GPU util 平均值 >60% | 必须 | [待填] | ⏸️ |
| GPU util P95 >50% | 必须 | [待填] | ⏸️ |
| VRAM 峰值 <90% | 必须 | [待填] | ⏸️ |
| 1小时无OOM/崩溃 | 必须 | [待填] | ⏸️ |
| 吞吐无持续性塌陷 | 必须 | [待填] | ⏸️ |
| 5个定义实验通过 | 必须 | [待填] | ⏸️ |

### 最终裁决

```
[ ] FAIL - 未满足 P0 标准，禁止进入 heterogeneous/
[ ] CONDITIONAL - 部分满足，需修复后重测
[ ] PASS - 全部满足，允许解锁进入 heterogeneous/
```

**解锁日期**: [待填]  
**解锁签名**: [待填]

---

## 附录: 5个定义实验清单

| # | 实验名 | 脚本 | 时长 | 通过标准 |
|:-:|:-------|:-----|:----:|:---------|
| 1 | 基础连接 | `test_gpu_basic.py` | 15min | CUDA可用, 显存分配正常 |
| 2 | 模型加载 | `test_gpu_inference.py` | 30min | 成功加载, 推理正常 |
| 3 | 显存压力 | `test_vram_stress.py` | 30min | 无OOM, 自动释放正常 |
| 4 | 稳定性长测 | `test_gpu_stability.py` | 60min | 1小时无崩溃 |
| 5 | 指标验证 | `test_metrics_collection.py` | 15min | 所有指标可采集 |

**实验完成状态**: [待填: X/5]

---

*本报告模板确保 GPU P0 验收标准化、可复现、不可绕过。*
